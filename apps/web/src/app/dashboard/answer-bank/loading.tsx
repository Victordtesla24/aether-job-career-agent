/** Route loading skeleton for the Answer Bank (C-03, QA-v2). */
import { RouteSkeleton } from "../../../components/route-error";

export default function AnswerBankLoading() {
  return <RouteSkeleton section="Answer Bank" />;
}
