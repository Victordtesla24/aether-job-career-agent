/** Route loading skeleton for Applications (C-03, QA-v2). */
import { RouteSkeleton } from "../../../components/route-error";

export default function ApplicationsLoading() {
  return <RouteSkeleton section="Applications" />;
}
