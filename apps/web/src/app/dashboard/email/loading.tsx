/** Route loading skeleton for Email Center (C-03, QA-v2). */
import { RouteSkeleton } from "../../../components/route-error";

export default function EmailLoading() {
  return <RouteSkeleton section="Email Center" />;
}
