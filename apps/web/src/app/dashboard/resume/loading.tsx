/** Route loading skeleton for Resume Studio (C-03, QA-v2). */
import { RouteSkeleton } from "../../../components/route-error";

export default function ResumeLoading() {
  return <RouteSkeleton section="Resume Studio" />;
}
