# Runtime dependencies for the Aether API.
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic[email]>=2.7
pydantic-settings>=2.3
httpx>=0.27
beautifulsoup4>=4.12
pdfplumber>=0.11
# Auth (P2-S01)
passlib[bcrypt]>=1.7
bcrypt==4.0.1  # pinned: passlib 1.7.x is incompatible with bcrypt>=4.1
python-jose[cryptography]>=3.3
# Database access — raw SQL over the Prisma-migrated schema (D-0013)
psycopg2-binary>=2.9
cuid>=0.4  # Prisma-compatible id generation for raw inserts
# ATS scoring engine (P2-S03) — deterministic, no LLM.
# scikit-learn provides TF-IDF keyword coverage; sentence-transformers provides
# the all-MiniLM-L6-v2 semantic-similarity embeddings (a pinned public checkpoint,
# downloaded once to SENTENCE_TRANSFORMERS_HOME). torch (CPU) is pulled in
# transitively by sentence-transformers.
numpy>=1.26
scikit-learn>=1.4
sentence-transformers>=2.7
