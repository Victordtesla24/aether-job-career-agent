jobs:
  x:
    continue-on-error: true
    steps:
      - run: pnpm run test || true
      - run: |
          if [ -z "$SECRET" ]; then
            echo "not set — skipping"
            exit 0
          fi
