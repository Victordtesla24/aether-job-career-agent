# D-ALERT: fire an operator alert email whenever aether-email-agent.service
# fails. See docs/delivery/OPS-ALERTING.md for the install + test-fire
# recipe. This unit is not deployed yet (SESSION-COORDINATION.md B5, still
# CLAIMED as deploy/-only) — this drop-in is staged ahead of it landing, same
# as the other four, so no follow-up drop-in PR is needed once it ships.
[Unit]
OnFailure=aether-alert@%n.service
