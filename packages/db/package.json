{
  "name": "@aether/db",
  "version": "0.1.0",
  "private": true,
  "description": "Canonical DB schema-of-record (src/schema.prisma). The runtime TS client layer was removed in the Workstream C dedup (nothing deployed imported it); apps/api owns the live schema via raw psycopg2 + lazy DDL and documents drift against this file.",
  "prisma": {
    "schema": "src/schema.prisma"
  }
}
