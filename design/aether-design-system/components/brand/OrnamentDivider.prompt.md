Use `OrnamentDivider` to close a centred heading on a ceremonial surface.

```jsx
<h2 className="type-display text-gilt">Simple, honest pricing</h2>
<OrnamentDivider width={240} />
```

One per band at most. Never stack it with another rule.
