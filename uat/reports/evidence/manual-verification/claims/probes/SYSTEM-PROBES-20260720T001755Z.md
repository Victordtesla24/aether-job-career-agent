# System-level fresh probes — claim-auditor consolidation pass

**Timestamp (UTC):** 2026-07-20T00:17:55Z
**Production:** https://5cb5f0620.abacusai.cloud (commit d313d23)
**Method:** curl against production, systemctl/git/grep on repo host, per DEPLOYMENT-RUNBOOK.md

## CLM-001 — health endpoint
`curl -sf .../api/health` -> `{"status":"ok","version":"0.2.0"}` HTTP 200. **CONFIRMED.**

## CLM-002 — systemd units active
`systemctl is-active aether-api aether-web aether-worker redis-server` -> all 4 `active`. **CONFIRMED.**

## CLM-004 — repo state
- `git branch -a`: local has 32 fix/* branches + main + many worktree-agent-* branches (NOT "exactly 1 branch").
- `git rev-list --left-right --count origin/main...main`: 0 behind / **78 ahead** of origin/main (local main not yet pushed).
- `gh pr list --state open`: 0 open PRs. CONFIRMED sub-claim.
- `git status --short`: 21 uncommitted paths (agent-config files, evidence docs from this run).
- **Verdict basis:** claim ("repo is clean: local main only, origin refs/heads/main only, 0 open PRs, exactly 1 branch") is REFUTED as literally stated for CURRENT mid-run state — this matches the orchestrator's known-context note that final state is re-confirmed at gate G-08 (not yet reached). 0-open-PRs sub-claim holds.

## CLM-019 — aether-discovery.timer end-to-end
See dedicated probe file `CLM-019-discovery-cron-FRESH-REFUTATION-*.md` in this directory. **REFUTED** (timer fires on schedule, but the triggered service has failed at login (HTTP 401) on every attempt for >24h of timestamped log coverage, most likely because the DB-wipe-affected cron account was never restored). New finding needed — not present in MANUAL-VERIFICATION-GAPS.json.

## CLM-020 — 23 duplicate PDF/DOCX exports removed, no .md changed
- `git log --oneline --all --diff-filter=D -- '*.pdf' '*.docx'`: exactly ONE commit in the entire history removed pdf/docx files — `c07e06d "chore(dir): remove 23 duplicate pdf/docx exports of canonical .md docs (GATE-23, ADR-P7-06)"` — and its `--numstat` shows only **5** files deleted (docs/research/{careerops_analysis,competitive_features,github_trackers_analysis,jobpilot_analysis,recruitment_and_apis}.pdf), not 23.
- `git show --numstat c07e06d`: 0 .md files touched. CONFIRMED sub-claim.
- Fresh scan of current tree (excl. node_modules/.claude/worktrees) for any .md file with a same-basename .pdf/.docx sibling: **zero matches** — no live duplicates today.
- **Verdict basis:** PARTIALLY-TRUE — the substance (dupes removed, .md untouched, zero dupes currently) holds, but the specific count "23" is not corroborated by git history (only 5 verifiable deletions found across all commits).

## CLM-021 — Phase-7 env vars present in production .env (names only, no values logged)
9 of 10 present: CLAUDE_CODE_OAUTH_TOKEN, AETHER_ALLOWED_INTERNAL_EMAIL_DOMAINS, AETHER_REDIS_URL, AETHER_REDIS_PASSWORD, AETHER_ASYNC_GENERATION, AETHER_LLM_WORKER_BUDGET_SECONDS, AETHER_LLM_WORKER_COVER_BUDGET_SECONDS, AETHER_LLM_WORKER_PIPELINE_BUDGET_SECONDS, AETHER_SYSTEM_RUN_SECRET all PRESENT. **AETHER_JOB_STALE_SECONDS MISSING** from .env (code has a safe default of 900s at apps/api/app/routers/agents.py:1082, so behavior is unaffected, but the claim says the var is "present"). **PARTIALLY-TRUE** (9/10).
AETHER_ASYNC_GENERATION value confirmed = `true` (name+value, not a secret). Supports CLM-003.

## CLM-028 — zero non-prod code reachable in prod build
Scoped grep of apps/web/src and apps/api/app (excluding tests/__tests__/fixtures): 0 `Math.random()` calls in reachable code (1 hit is a code COMMENT explicitly documenting its absence), 0 TODO/FIXME in reachable src, 0 mock/stub markers. **CONFIRMED** (scoped spot-check, not exhaustive).

## CLM-030 — no secret leakage into committed/pushable paths
`git grep` for the `sk-ant-oat01-` prefix pattern across tracked files + `git log --all -p -S`: all 18 hits are code/doc/test references to the prefix-matching LOGIC (e.g. `.startswith("sk-ant-oat01-")`), not an actual leaked token value. Grep for the production DB host substring in tracked files: 0 hits. **CONFIRMED** (no full secret values found in git-tracked history or working tree).

## CLM-079 / CLM-011 — OAuth-start endpoint removed
`curl GET/POST .../api/agents/auth/anthropic/start` -> 404/404 (fresh, this probe). **CONFIRMED**, corroborates tester evidence.

## CLM-089 — admin routes 401 unauthenticated
Fresh curl (no auth) to /api/admin/{health,users,settings,audit-log,spend} -> all 401. **CONFIRMED**, corroborates MV-admin-settings-002 + tester evidence (403-for-non-admin-authenticated portion not independently re-probed here, no non-admin token available without creating a new account).

## CLM-095 — Stripe webhook rejects unsigned payload
`curl -X POST .../api/billing/webhooks/stripe` with a JSON body and no `stripe-signature` header -> HTTP 400 `{"detail":"Missing stripe-signature header"}`. **CONFIRMED** for this minimum sub-claim (raw-body-before-parsing internals and idempotency-on-replay remain UNVERIFIABLE-FROM-UI without real Stripe test keys, per PHASE6-BLOCKED-ON-HUMAN.md).

No secrets logged (values shown above are non-secret: HTTP status codes, boolean env value, counts, public API-prefix patterns).

## CLM-016 — aether-worker.service unit details (probed 2026-07-20T00:18:05Z)
`systemctl show aether-worker.service`: ActiveState=active. Unit file at /etc/systemd/system/aether-worker.service (matches deploy/aether-worker.service in repo) shows `Requires=redis-server.service` and `StandardOutput/StandardError=append:/var/log/aether/worker.log` exactly as claimed. `tail /var/log/aether/worker.log` shows live activity at 00:15:00 UTC (cron:sweep_stale_jobs), i.e. actively running right now. **CONFIRMED.**
