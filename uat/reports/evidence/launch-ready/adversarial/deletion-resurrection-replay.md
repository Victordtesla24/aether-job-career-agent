# Deletion Resurrection Replay — Workstream F (2026-07-24)

Sampled ≥25% of every deletion manifest (seed 20260724 for the path samples)
and replayed each: deleted symbols greped for live references across
`apps/web/src`, `apps/api/app`, `scripts`, `ci`; deleted paths checked absent.

## Sample sizes
- W-C dedup symbols: 13 sampled (of 19 deletion candidates' symbol sets) —
  8 wave-1 dead API fns, barrel/test helpers, 3 deleted type decls, ml-*
  playwright configs, plus the whole `packages/*` TS-layer import check.
- W-D repo manifest: 6/18 items; W-D home manifest: 21/65 listed paths (~32%).

## Results
- **W-C symbols: 13/13 CLEAN.** Three initial substring hits were adjudicated
  false positives with word-boundary re-greps (all empty):
  `fetchResume` vs surviving `fetchResumes`/`fetchResumeDiff`;
  `GstBreakdown` (deleted type) vs surviving in-file `GstBreakdownSchema` const;
  `GmailAccount` (deleted FE type) vs the unrelated Python
  `GmailAccountRepository`. `@aether/agents|queue|shared` imports: 0. Barrel
  `lib/auth` import: 0.
- **W-D repo paths: 6/6 correct.** `.env.bak-predeploy`, caches, evicted PNG
  all absent; `uat/` dir itself and `apps/api/.pytest_cache` exist by design
  (manifest excluded launch-ready evidence; pytest cache regenerated by this
  run's live full suite and is gitignored — verified `git check-ignore`).
- **W-D home paths: 20/21 absent. 1 genuine miss found and FIXED this run:**
  `/home/ubuntu/OpenRouter-Custom-Router-Config.docx` was manifested
  "archive-to-S3-then-delete" but still existed. Remediation (§9): uploaded to
  `s3://…/49362/launch-ready-evidence/home-archive/OpenRouter-Custom-Router-Config.docx`
  (16.6 KiB, upload confirmed) then deleted; path now absent.

## Verdict
No live references to any deleted code (0 resurrection blockers). One
file-eviction miss (severity LOW, no code impact) found by the replay and
closed in-run per the §9 pipeline.
