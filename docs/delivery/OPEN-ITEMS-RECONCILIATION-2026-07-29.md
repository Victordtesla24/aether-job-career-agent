# Open-Items Reconciliation — 2026-07-29

**Method:** documentation reconciliation pass only — no production re-verification performed by
this pass (that is the job of `evidence`/`qa`/`qa-adversary` agents on separate dispatch). Every
row below is drawn from a claim already recorded in `docs/delivery/*.md`/`*.json`, cross-checked
against sibling documents and, where the claim concerned code state, against the actual working
tree (`git status`/`git diff` — read-only, no files were changed by this pass). Scope: all
non-archived `docs/delivery/*.md`/`*.json`, with `docs/delivery/archive/README.md` read only to
confirm which archived files are declared superseded.

**Files scanned:** `MODELS-LIVE-GAPS.json` (87 findings), `LAUNCH-READY-STATE.json`,
`LAUNCH-READY-BLOCKED-ON-HUMAN.md`, `LAUNCH-READY-FINAL-REPORT.md`,
`LAUNCH-READY-GOVERNANCE-AUDIT.md`, `MODELS-LIVE-FINAL-REPORT.md`,
`EXTERNAL-CLIENT-ACCESS-FIX-2026-07-29.md`, `GAP-P4-003-RCA.md`, `PHASE4-GAP-ANALYSIS.md` (current,
2026-07-26 — distinct from the same-named archived Phase-4 doc), `INCIDENT-2026-07-21-*.md`,
`INCIDENT-PROD-DB-WIPE-2026-07-18.md`, `ADR-MV-02-paywall-marketing.md`,
`ML-agents-cred-002-BLUEPRINT.md`, `DECISIONS.md`, `PROGRESS.md`, `TRACEABILITY-MATRIX.md`,
`REQUIREMENTS-TRACEABILITY-PRODUCTION.md`, `DEPLOYMENT-RUNBOOK.md`, `archive/README.md`.
`uat/reports/evidence/launch-ready/` was consulted only as the evidence-path targets already cited
by the docs above (not independently opened/verified).

---

## Table (sorted by severity)

| # | ID | Source (file:line) | Description | Recorded status + date | Classification | Notes |
|---|----|--------------------|--------------|------------------------|-----------------|-------|
| 1 | GAP-P4-002 | `PHASE4-GAP-ANALYSIS.md:184-195` | Authenticated agent-activity feed (`/dashboard`, `/dashboard/agents`) surfaced `Fabricated entities detected: ['origination']` — a fabricated entity in what must be a real run/activity feed. | **OPEN**, 2026-07-26 | **CODE-FIXABLE-NOW** | A fix is already **written but uncommitted** in the working tree (`apps/api/app/routers/agents.py`, `git diff` shows new `except FabricationError`/`except StructuralError` handlers that record an honest `completed` degrade instead of a fabricated-entity failure row, tagged `# GAP-P4-002` in the diff itself). Not yet tested-suite-verified, committed, or deployed — see OBS-EXT-004 below; this is the same underlying work item. |
| 2 | QA-RES-002 | `EXTERNAL-CLIENT-ACCESS-FIX-2026-07-29.md:33` | Entire LLM pipeline degraded in production: OpenRouter account has insufficient credits (HTTP 402); 1,713+ failed `cover_letter` calls; board-sweep autopilot degrades "gracefully" but produces zero output. `.env` has no direct Anthropic key, so OpenRouter is the *only* live LLM path. | **OPEN** ("pre-existing"), 2026-07-29 | **HUMAN-GATED** | Operator must: (a) top up OpenRouter credits, or (b) add a direct `AETHER_LLM_API_KEY` to `.env`, or (c) explicitly authorize billing the Abacus VM LLM endpoint through the subscription. No code path can route around this. |
| 3 | GAP-P4-001 | `PHASE4-GAP-ANALYSIS.md:171-182` | Stage-A test scouts could not establish an authenticated production session ("available credentials were rejected"); blocked independent authenticated UI/API coverage on 15 routes. | **OPEN**, 2026-07-26 | **CODE-FIXABLE-NOW** | Fix spec: provision a dedicated least-privilege test account/storage-state for scouts, in an authorized secret channel. **Contradiction flagged** — see Contradictions §7. |
| 4 | QUALITY-WE-006 (aka RT-live-payment) | `MODELS-LIVE-GAPS.json:1642`; `LAUNCH-READY-BLOCKED-ON-HUMAN.md:7-30` | Live Stripe payment completion → webhook → entitlement flip, and portal cancel/downgrade, have never been exercised with a real card; everything short of that is verified live. | **CONDITIONALLY-CLOSED**, 2026-07-24 | **HUMAN-GATED** | Operator must complete one real (or Stripe test-clock) Checkout + exercise cancel/downgrade in the hosted portal per the runbook in `LAUNCH-READY-BLOCKED-ON-HUMAN.md` §1. |
| 5 | RT-003 (Adzuna AU) | `MODELS-LIVE-GAPS.json:1731`; `LAUNCH-READY-BLOCKED-ON-HUMAN.md:32-58` | AU job-discovery volume saturated at ~37 mostly-global postings (persisted ≈0/day); the licensed, ToS-compliant Adzuna AU adapter is fully implemented but skipped because free-tier credentials are absent. | **CONDITIONALLY-CLOSED**, 2026-07-24 | **HUMAN-GATED** | Operator must register at developer.adzuna.com, add `ADZUNA_APP_ID`/`ADZUNA_APP_KEY` to `.env`, restart `aether-api`/`aether-worker`. |
| 6 | GAP-P4-003 | `PHASE4-GAP-ANALYSIS.md:197-208` | Live funnel `50 jobs → 42 applied → 2 screened → 0 interviewed → 0 offers` (0% interview/offer conversion) filed as a "material outcome/learning gap." | Ledger row still literally reads **OPEN**, 2026-07-26 | **STALE** — superseded by `GAP-P4-003-RCA.md` (same day) | RCA traced the calculation end-to-end (`analytics.py`, `applications.py`) and concluded **"no stage-progression code defect confirmed... expected behavior / product-integration gap"** — there is deliberately no automated employer-outcome source, so 0% is mathematically correct, not a bug. **Contradiction flagged** — see Contradictions §2. Recommended next action per the RCA (record a verified employer response or connect an outcome-event source) is a product decision, not a code defect. |
| 7 | Operator admin credential / 2nd Gmail consent / Anthropic OAuth operator consent | `LAUNCH-READY-STATE.json:253`; `LAUNCH-READY-FINAL-REPORT.md:68-70` | Three items named as human-gated next actions in the state file and final report. | Implied **OPEN/human-gated**, 2026-07-24 | **HUMAN-GATED** | No dedicated remediation instructions exist anywhere for these three (unlike Stripe/Adzuna, which have full `LAUNCH-READY-BLOCKED-ON-HUMAN.md` sections). **Contradiction flagged** — see Contradictions §4. The Anthropic-OAuth item is the human-consent half of `ML-agents-cred-002` (already `VERIFIED-CLOSED` code-side, `MODELS-LIVE-GAPS.json:70-93`, human-gated residual = "operator consent + real code exchange + real subscription-billed run"). |
| 8 | GAP-P4-004 | `PHASE4-GAP-ANALYSIS.md:210-221` | `/dashboard/analytics` Market Pulse reports "Market data: not connected" — no real market-telemetry source wired. | **OPEN**, 2026-07-26 | **CODE-FIXABLE-NOW** (partial WIP) | Uncommitted `git diff` on `apps/web/src/components/analytics/MarketPulse.tsx` adds an honest "External market benchmark unavailable — Provider: none configured" banner (disclosure fix, matches the RCA's own recommended path: "retain a clearly labelled unavailable state ... document an ADR if market integration is not committed"). No real market-data source has been wired and no ADR has been written yet — the underlying "is a market-data integration in scope at all" product decision is still unresolved. |
| 9 | QA-RES-001 | `EXTERNAL-CLIENT-ACCESS-FIX-2026-07-29.md:34` | `/dashboard/email` shows skeletons ~11–12s; server logs show a Gmail credential cache-miss causing a `401 → retry` twice per request. | **OPEN**, 2026-07-29 | **CODE-FIXABLE-NOW** | Fix requires an `aether-api` deploy, which is itself deliberately blocked pending resolution of the untested WIP described in OBS-EXT-004 (a restart would activate untested code). Sequencing dependency, not a missing capability. |
| 10 | OBS-EXT-004 | `EXTERNAL-CLIENT-ACCESS-FIX-2026-07-29.md:17` | Repo sits on branch `fix/remove-fabricated-origination-activity`, 2 commits ahead of `origin/main`, with ~15 uncommitted WIP files from another in-flight session; 4 WIP API files were edited *after* the running `aether-api` started, so the healthy prod API runs pre-WIP code. `aether-api` deliberately not restarted. A VM reboot would activate the untested WIP. | **FLAGGED**, 2026-07-29 | **CODE-FIXABLE-NOW** (operational risk) | Verified independently this pass: `git status` on the live repo confirms the branch and the 4 modified files (`db.py`, `story.py`, `agents.py`, `dedup.py`) plus 6 modified web files and 4 untracked files. This is the **same work item** as GAP-P4-002, GAP-P4-004, and the undocumented story-dedup finding below — should be tracked as one consolidated fix, not four. Residual risk: an unplanned VM restart activates untested code in production. |
| 11 | Undocumented story-dedup fix (`G-P4-STORY-DEDUP-004`) | found via `git diff apps/api/app/repositories/story.py`, `apps/api/app/db.py`, `apps/api/app/services/dedup.py`; test file `apps/api/tests/test_story_dedup.py` (untracked, dated Jul 26 23:56) | Story-bank `create()` now hashes the 5 STAR fields + userId and skips duplicate inserts. The code cites gap ID `G-P4-STORY-DEDUP-004` in a docstring. | Not present in **any** ledger | **CODE-FIXABLE-NOW** (governance gap) | Grepped `MODELS-LIVE-GAPS.json`, `PHASE4-GAP-ANALYSIS.md`, and every archived ledger for `STORY-DEDUP`/`G-P4-STORY` — zero hits. A real defect was found and is being fixed entirely **off-ledger**, with no discoverable finding record. Flag for the orchestrator: back-fill a ledger row before/at the point this is committed. |
| 12 | DEDUP-044 | `LAUNCH-READY-FINAL-REPORT.md:78`; `LAUNCH-READY-GOVERNANCE-AUDIT.md:32` | Behavior-changing "lazy-DDL merge" dedup candidate, classified RISKY by the de-dup discovery workflow, deliberately deferred rather than executed. | **Deferred**, 2026-07-24 | **HUMAN-GATED** (needs explicit sign-off given RISKY/behavior-changing label) | No ledger row exists for `DEDUP-044` itself in `MODELS-LIVE-GAPS.json` — only narrative mentions in two reports. Low urgency (deliberately deferred, not blocking); flagged so it isn't silently dropped. |
| 13 | OBS-EXT-003 | `EXTERNAL-CLIENT-ACCESS-FIX-2026-07-29.md:16` | `admin/admin123` (the credential given to external business-client testers) resolves to the **owner's real personal account** (sarkar.vikram@gmail.com) — clients see the owner's real job-search data. | **FLAGGED to owner**, 2026-07-29 | **HUMAN-GATED** | Credential deliberately left unchanged pending an owner decision on a sanitized demo account. Data-exposure tradeoff, not a code defect. |
| 14 | ML-networking-001 | `MODELS-LIVE-GAPS.json:539` | Networking screen has a working backend `DELETE` endpoint for contacts but no UI delete control. | **CONDITIONALLY-CLOSED** ("UNSURE... Adjudicate at Stage-3"), originally 2026-07-22 | **CODE-FIXABLE-NOW** (trivial UI-affordance decision) | **Contradiction flagged** — see Contradictions §6: no later document records the promised Stage-3 adjudication ever happening. |
| 15 | BACKLOG-P6-02 | `REQUIREMENTS-TRACEABILITY-PRODUCTION.md:~254` (doc tail, "Honest backlog") | ~20% of tailoring/cover-letter runs return an honest 503 under the fixed ~100s synchronous HTTP edge timeout; durable fix identified as asynchronous submit→poll generation, explicitly out of Phase-6 scope. | Disclosed **non-blocking backlog**, 2026-07-16 | **CODE-FIXABLE-NOW** (low priority) | Still synchronous as of the newest evidence — the 2026-07-24 runtime wave (RT-002) only widened budgets, it did not make generation asynchronous. Archived `PHASE7-ASYNC-BLUEPRINT.md` exists but its implementation status could not be confirmed from any non-archived doc in scope; flag for orchestrator follow-up rather than assume closure. |
| 16 | GAP-P6-REPO-002 | `REQUIREMENTS-TRACEABILITY-PRODUCTION.md` (doc tail) | Stale branches / open PRs needed cleanup. | "TRIAGED (Cluster H, deployer)", 2026-07-12 | **STALE** | Superseded by `LAUNCH-READY-GOVERNANCE-AUDIT.md:46-48`: fresh 2026-07-24 check — `git ls-remote --heads origin` → `main` only, `gh pr list` → 0 open PRs. |
| 17 | GAP-P6-DIR-001 | `REQUIREMENTS-TRACEABILITY-PRODUCTION.md` (doc tail) | Monorepo directory reorganisation needed. | "TRIAGED (Cluster H, fixer-medium)", 2026-07-12 | **STALE** | Superseded by Launch-Ready Workstream W-C (2026-07-24): 66 tracked files hard-deleted, orphaned `packages/*` TS layer removed, net −3,670 LOC (`LAUNCH-READY-STATE.json:148-159`). |
| 18 | GAP-P6-DOCS-001 / GAP-P6-EXEC-001 | `REQUIREMENTS-TRACEABILITY-PRODUCTION.md` (doc tail) | Docs needed refresh to post-implementation truth; `EXECUTION-REPORT.md` claims needed re-verification. | "TRIAGED → in progress", 2026-07-12 | **STALE** | Superseded by multiple later doc-truth passes: W-D docs consolidation (2026-07-24, 38 files archived) and W-F's 12/12 fresh claim spot-audit (`LAUNCH-READY-FINAL-REPORT.md:17`). |
| 19 | DEDUP-DISCOVERY-COMPLETE | `MODELS-LIVE-GAPS.json:1416` | 53-candidate de-dup discovery inventory record. | **INVENTORY-READY**, undated (discovery-phase artifact) | **STALE** | Self-declared "consumed by W-C" in `MODELS-LIVE-FINAL-REPORT.md:39`; W-C dedup workstream closed 2026-07-24. |
| 20 | ML-model-003 | `MODELS-LIVE-GAPS.json:520` | Default reasoning model (`deepseek/deepseek-v4-pro`) had 1 transient malformed response in 82 probes; system handled it correctly (honest 503 + refund, no silent fallback). | **CONDITIONALLY-CLOSED** (explicitly labeled "not a defect"), 2026-07-22-ish | **STALE** (no action needed) | Included for 100% adjudication completeness only — this is an operational-monitoring note, not a work item. |

---

## Contradictions

1. **"Launch ready / complete" vs. immediate production breakage.** `LAUNCH-READY-STATE.json` (`phase_step: "COMPLETE"`, all 6 workstreams `closed`, `LAUNCH-READY-FINAL-REPORT.md:82-85`: *"Aether is ready for real paid user onboarding"*) is dated **2026-07-24**. Two days later, `PHASE4-GAP-ANALYSIS.md` (2026-07-26) opens 4 fresh gaps including a **CRITICAL** fabrication defect (GAP-P4-002) and states *"NOT READY FOR STAGE-C EXIT"* (`PHASE4-GAP-ANALYSIS.md:258`). Five days after that, `EXTERNAL-CLIENT-ACCESS-FIX-2026-07-29.md` finds `/dashboard/jobs` **fully blank for every external client** (DEF-EXT-001, BLOCKER) due to a stale-build-no-restart bug that had apparently been live since 2026-07-26 23:57 — directly undercutting the G-G (clean observation window) and G-H (suites green) "launch ready" exit-gate claims of production health at the time real clients tried to use it.

2. **GAP-P4-003 ledger status vs. its own RCA.** `PHASE4-GAP-ANALYSIS.md:207` still literally reads `**Status:** OPEN`, but the same-day companion document `GAP-P4-003-RCA.md:4` reads `**Status:** Investigated — no stage-progression code defect confirmed`. The ledger row was never edited to reflect the RCA's conclusion; the two documents disagree on the record as they stand.

3. **Canonical ledger is missing every finding from the last 3 days of work.** `docs/delivery/archive/README.md:4-6` names `MODELS-LIVE-GAPS.json` as *the* current, canonical ledger superseding all archived per-phase gap docs. That file (87 findings, checked exhaustively by `id`) contains **zero rows** for GAP-P4-001..004, DEF-EXT-001/002, QA-RES-001/002, or OBS-EXT-003/004. Anyone consulting only the canonical ledger — as the archive README instructs — would see "0 OPEN" and conclude the system is fully closed, missing at least one CRITICAL and two HIGH findings discovered since 2026-07-26.

4. **"Blocked on human" doc is incomplete relative to its own sibling docs.** `LAUNCH-READY-BLOCKED-ON-HUMAN.md:1-4` states *"Only credentials/actions that genuinely require the operator are listed here... Everything else... is agent-executable."* It documents exactly 2 items (Stripe live payment, Adzuna AU credentials). But `LAUNCH-READY-STATE.json:253` and `LAUNCH-READY-FINAL-REPORT.md:68-70` both name **3 additional** human-gated items (operator admin credential, 2nd Gmail consent, Anthropic OAuth operator consent) that appear nowhere in the "blocked on human" document itself.

5. **A defect is being fixed under a gap ID that exists in no ledger.** The uncommitted `apps/api/app/repositories/story.py` diff cites gap ID `G-P4-STORY-DEDUP-004` in its module docstring. This ID does not appear in `MODELS-LIVE-GAPS.json`, `PHASE4-GAP-ANALYSIS.md`, or any archived ledger checked. A test file (`apps/api/tests/test_story_dedup.py`) exists for it, but the finding itself was never filed anywhere discoverable.

6. **ML-networking-001 marked closed but never adjudicated.** `MODELS-LIVE-GAPS.json:539` status is `CONDITIONALLY-CLOSED`, yet its own `summary` field reads *"UNSURE... Could be intentional... Adjudicate at Stage-3."* `MODELS-LIVE-GOVERNANCE-AUDIT.md:71` (archived) confirms it was flagged as "1 LOW UNSURE" and no later document — checked `LAUNCH-READY-*`, `MODELS-LIVE-FINAL-REPORT.md`, `PHASE4-GAP-ANALYSIS.md` — records the promised Stage-3 adjudication ever taking place. The "closed" label appears to be a placeholder rather than an actual decision.

7. **GAP-P4-001's premise vs. 3-days-later evidence.** `PHASE4-GAP-ANALYSIS.md:175` (2026-07-26) reports Stage-A scouts had "available credentials... rejected" at `/login`. `EXTERNAL-CLIENT-ACCESS-FIX-2026-07-29.md:20` cites a pre-fix repro (`REPRO-VERDICT.json`) stating *"login WORKS desktop+mobile"* with `admin/admin123` via Playwright, three days later. Neither document acknowledges the other; it is unresolved from documentation alone whether GAP-P4-001 reflects a scout-tooling defect (stale/wrong credential in that specific run) or a real, since-changed condition. GAP-P4-001's own fix spec (provision a *dedicated least-privilege* scout account, distinct from the shared owner-linked `admin/admin123` flagged separately in OBS-EXT-003) remains unactioned either way.

---

## Counts

- **CODE-FIXABLE-NOW:** 8 (GAP-P4-002, GAP-P4-001, GAP-P4-004, QA-RES-001, OBS-EXT-004, undocumented story-dedup fix, ML-networking-001, BACKLOG-P6-02)
- **HUMAN-GATED:** 6 (QA-RES-002, QUALITY-WE-006/Stripe, RT-003/Adzuna, operator-admin+2nd-Gmail+Anthropic-OAuth-consent bundle, DEDUP-044, OBS-EXT-003)
- **STALE:** 6 (GAP-P4-003, GAP-P6-REPO-002, GAP-P6-DIR-001, GAP-P6-DOCS-001/EXEC-001, DEDUP-DISCOVERY-COMPLETE, ML-model-003)
- **Total rows adjudicated:** 20 (100% — no blank classifications)
- **Contradictions found:** 7

## Top 5 code-fixable items (by severity)

1. **GAP-P4-002** — fabricated `origination` entity surfaced in the agent-activity feed (CRITICAL). Fix already drafted in the uncommitted working tree (`apps/api/app/routers/agents.py`); needs test-suite verification, commit, and a deliberate `aether-api` deploy.
2. **GAP-P4-001** — no dedicated authenticated test/scout account exists, blocking independent verification coverage (HIGH); trivial to provision but flagged for a real credential-tooling contradiction (Contradiction §7) before assuming it's still broken.
3. **QA-RES-001** — `/dashboard/email` ~11–12s load from a Gmail credential cache-miss causing double 401-retries (MEDIUM); fix is sequenced behind resolving the WIP branch (OBS-EXT-004).
4. **OBS-EXT-004 / undocumented story-dedup fix / GAP-P4-004** — three items that are really **one** in-flight work item: an uncommitted branch (`fix/remove-fabricated-origination-activity`) sitting live on the VM, 2 commits ahead of `origin/main`, that a VM reboot would silently activate untested. Needs the orchestrator to explicitly decide: finish testing + commit + deploy deliberately, or revert.
5. **ML-networking-001** — Networking screen missing a contact-delete UI control despite a working backend endpoint (LOW); a 5-minute adjudication call that was promised at "Stage-3" and never made.
