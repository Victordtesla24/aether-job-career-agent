# Production Hardening Run — 2026-07-29 (Run 2)

Operator mandate: admin-only free-model fallback on OpenRouter 402; independent adversarial review;
fix ALL open issues (including pre-existing); push; re-verify production. Real paying subscribers live.
Orchestrator: brain-only; all execution delegated. Prior run: EXTERNAL-CLIENT-ACCESS-FIX-2026-07-29.md.

## Workstreams

| WS | Item | Status |
|----|------|--------|
| W-1 | Admin-gated OpenRouter free-model fallback on HTTP 402 (llm_client.py only; nemotron-3-super-120b/ultra-550b `:free` verified live at 0 credits; existing broken `openai/gpt-oss-20b:free` constant replaced where used). Design: 402→`InsufficientCreditsError`; isAdmin via user_id contextvar + 60s-TTL lookup; same _call_live path + downstream quality gates; non-admin byte-for-byte unchanged. | IN-FIX (opus fixer) |
| W-2 | QA-RES-001 Gmail credential refresh per request (11.9s inbox). RCA: Credentials built without expiry= (gmail_service.py:219-228); dead refresh-and-persist branch. Fix v2 after adversarial FAIL→re-fix cycle: expiry= (naive-UTC), TransportError sibling handler, _client() inside honest-error scope in list_threads+send ("no email was sent" 502 guarantee restored), missing-OAuth-config → GmailError (not "reconnect"), aware-UTC persist hardening ×2 sites, fresh-expiry-never-refreshes mutant-killer test. Reviewer final PASS (probe-verified, +oauth suites 33/33). Evidence: qa-res-001-* + qa-res-001-review-verdict.json. | FIXED-AWAITING-QA (review PASS) |
| W-3 | WIP-branch reconciliation per audit (WIP-BRANCH-AUDIT-2026-07-29.json). Rulings R-1..R-5 below. | RULED, awaiting fixer slot |
| W-4 | Deep interaction sweep of production: COMPLETE 04:15Z — 10/11 areas clean at interaction level (102 screenshots, INTERACTION-FINDINGS.json). 2 findings: ML-EMAIL-001 (12.3s email load, = QA-RES-001/W-2 in-fix) + ML-SETTINGS-001 (HIGH, NEW: Privacy & Compliance tab activates but renders Profile panel — no privacy content; ×3 reproduced). Throwaway signup qa-deepsweep-20260729@example.com left for cleanup at close. | COMPLETE → ML-SETTINGS-001 IN-FIX |
| W-5 | Ledger-of-record refresh: MODELS-LIVE-GAPS.json has no rows since 2026-07-26 (docs auditor). Append this run's rows at close. | PENDING |
| W-6 | Email inbox N+1 (≤25 sequential threads().get() per account) — evaluate after W-2 live latency measurement. | PENDING MEASUREMENT |

## Orchestrator rulings on WIP branch (binding; evidence = WIP-BRANCH-AUDIT-2026-07-29.json)

- **R-1 REVERT api WIP**: apps/api/app/{db.py, repositories/story.py, routers/agents.py, services/dedup.py} → back to HEAD 5f0af49. agents.py NameError (undefined FabricationError/StructuralError at :785/:805) makes every lower except unreachable → refunds (_refund_once) and 503/429 mapping break on next api restart; 2 pre-existing tests prove it. story-dedup half-done (update() lacks ensure-column → prod 500; sha256 leak; no PUT test). Park full diff as patch + `parked/` copy of tests for honest re-land.
- **R-2 REVERT analytics fabrication**: apps/web/src/app/dashboard/analytics/page.tsx tooltip + its edited test — copy claims drafts excluded while backend counts ALL DISTINCT jobIds (test_analytics.py:230-232 proves 3 drafts + 4 submitted → 7). Restores MV-analytics-005 honest copy. Fake-green test edit reverted with it.
- **R-3 REVERT paywall exemption**: apps/web/src/components/subscription-gate.tsx (+ its test edit) — exempts /dashboard/email claiming "backend endpoints protect privileged actions"; emails.py has ZERO entitlement references → live paywall hole. Restore last deliberately-shipped paywall. Exemption is an OWNER decision (needs backend enforcement first); filed as human-gated.
- **R-4 KEEP + FIX**: components/analytics/MarketPulse.tsx + test (audited exemplary, green both sides) and app/dashboard/applications/page.tsx safety banner — FIX `fa-shield-check` → `fa-shield-halved` (Pro-only glyph renders empty on Free FA 6.5.1) and add missing red gate-OFF branch test.
- **R-5 PARK** apps/api/tests/test_story_dedup.py → docs/delivery/parked/ (imports reverted code; would break collection). Untracked docs (GAP-P4-003-RCA.md, PHASE4-GAP-ANALYSIS.md) get committed as documentation.

## Additional orchestrator rulings (2026-07-29 ~04:30Z)

- **ADR-ML-3 AMENDMENT (binding):** admin-only, logged free-model substitution on OpenRouter 402 is AUTHORIZED even when the 402'd model was user-chosen. Rationale: the operator's explicit mandate is the consent ADR-ML-3's no-silent-substitution rule exists to protect; 402 is a billing impossibility no model choice can satisfy; the rescue is admin-gated, logged (`admin-free-fallback` marker), and kill-switchable (empty env). Non-admin runs: ADR-ML-3 untouched.
- **AUTHORIZED:** .env.example `AETHER_MODEL_FALLBACK` one-liner → live-verified working free id (old id produces garbled output at realistic length; probe evidence). Prod .env's paid fallback stays (quality for paying users).
- **FILED (open follow-up, NOT in tonight's deploy):** AgentRun rows record the requested model, not the free model that actually served a rescued run; only signal today is the INFO log marker. Needs a small agents.py change post-reconciliation.
- W-1 status: FIXED-AWAITING-REVIEW — 16 tests (fail-before 13/3-PINs, pass-after 16/16), 394/6 related suites with all 6 failures proven pre-existing (WIP agents.py NameError, already ruled R-1 REVERT) via HEAD-swap baseline.

## Rulings round 3 (post web-review FAIL on Target 2, ~04:45Z)

- **R-6 (reverses R-4-partial):** applications/page.tsx uncommitted banner rewrite + its 3 tests REVERTED & PARKED — banner claimed approvalGate/autoApply control agent behavior; both are persisted-but-inert (verified: zero backend readers; real gate = unconditional `_APPROVAL_GATED` agents.py:77). Tests pinned false strings (same anti-pattern as parked blocker #3). Never shipped (audit's "already live" claim falsified via `git log -S`). MarketPulse KEEP stands (independently re-verified honest).
- **R-7 / finding INERT-CONFIG-001 (HIGH honesty, PRE-EXISTING, live):** Settings' autoApply/approvalGate/matchThreshold controls save but enforce nothing (fail-safe direction). Tonight: truthful helper copy under the controls (settings fixer, test-first). Real wiring = designed follow-up, OWNER product decision (esp. autoApply semantics).
- Review verdicts FINAL (~05:20Z): W-1 PASS (no must-fix) · W-2 PASS (M1-M4 probe-verified closed) · web PASS rev-2 (Privacy panel TRUE-copy verified; R-6 revert/park verified closed; R-7 hints verified factually exact incl. per-agent ApprovalRequest enforcement at tailor_agent.py:413/cover_letters.py:781/email_agent.py:441; 588/588 vitest with reconciling arithmetic 587−3+4). Carried non-blocking: NTH-5 (autoApply toggle description sentence adjacent to hint — make conditional later) · DEF-1 resolved by W-1 review (logged marker) + ADR amendment + filed AgentRun-audit follow-up.
- Full pytest gate #1 (05:30Z): **10 failed / 1289 passed** — DEPLOY BLOCKED. Failures cluster on committed feature 14e30c5 (submission validation gate): test_applications_tracker submit, test_clear_pipeline ×5, test_rt_004 promotion guards ×2, test_scout_live_sources submit ×2. Prod's running API already includes 14e30c5 → failures are either stale pre-feature tests OR live feature bugs. **W-7 opened:** adjudication+fix dispatched (opus): prove pre-existing via 3-file stash, rule stale-test vs feature-bug per test (invariants — idempotency, cross-user isolation, audit rows — must survive any test update), then full-suite re-run as the deploy gate.
- **W-7 GATE GREEN (05:42Z): full suite 1299/1299, 0 failed** (identical collection count to baseline → nothing skipped/deleted; log full-suite-after-ml-adjudication-20260729T051330.log). Final tally: 7 of 10 were feature bugs (5×clear-pipeline shadowing + 2×submit gate), 3 stale tests. **Orchestrator ruling:** promotion-time tailored-resume resolution UPHELD (consistency with /jobs/{id}/apply, minimal, requirement preserved); cover_letter_agent attaching base-resume-at-creation filed as designed follow-up. Awaiting cross-model review verdict, then deploy.
- **W-7 adjudication (~05:45Z):** pre-existence PROVEN (stash isolation, SHA-verified restore). Rulings: 2 REAL BUGS fixed — (i) `DELETE /jobs/clear-pipeline` unreachable since introduction (route shadowed by earlier-registered `DELETE /{job_id}`; dead endpoint, UI uses `POST /applications/pipeline/clear`; shadowing detector reports 0 across routers post-fix); (ii) **submit gate unsatisfiable — every board "Mark as submitted" 422'd for every user** (drafts always attach base resume; gate demanded attached-is-tailored). Fixed via sibling-apply-style tailored-resume resolution; no-tailored-resume still hard-422s. 3 stale tests updated invariant-preserving. Cross-model review dispatched; full-suite re-run in progress (0 fails @7%).

## Deploy plan (single coordinated deploy after all fixes + reviews + gates)
1. All fixers done → adversarial reviews (cross-model) → full gates: `flock /tmp/aether-pytest.lock scripts/run-tests.sh` (single-process, ~34m), vitest full, `pnpm build`.
2. Commit(s) on branch → merge/push per operator standing deploy (origin main) → restart aether-api + aether-worker + aether-web (web restart immediately after build, §0.3; api restart now SAFE post-R-1).
3. Live verification: external QA sweep; live proof of W-1 (admin cover-letter run engages `admin-free-fallback` marker + produces gated output while credits exhausted); non-admin unchanged (mltest account or fresh signup → 402 path); W-2 inbox latency re-measure; 30-min monitored log window.

## Human-gated (cannot be code-fixed; reported to operator)
OpenRouter credit top-up (or Anthropic key) for PAID-model quality; Adzuna AU key (RT-003); Stripe live-payment re-verify (QUALITY-WE-006); Gmail second-account/OAuth consents; shared admin credential exposes owner's personal data (OBS-EXT-003); email-paywall exemption product decision (R-3).

## QA closure #1 (06:55Z): FAIL — 6/9 PASS → wave-2

PASS: external sweep 14/14 (no split-brain, buildId match), submit gate LIVE-proven (real 200 promotion of the previously-always-422 shape, app c6cad5fc5a8182877738f5d85), settings honesty live, analytics honesty live, clear-pipeline routable (422 not 404), 36-min logs clean. FAIL/found: QA-FAIL-01 free-chain engaged but nemotron reasoning tokens blew the ~50s budget (3/3 → honest 503; same prompt replayed in 22-32s); QA-FAIL-02 worker root logger WARNING → admin-free-fallback marker invisible; QA-FAIL-03 autopilot tick 8.7s→550s processing 0; QA-RES-F (HIGH pre-existing) feed labels 1,862 degraded cover runs "Drafted"; email still ~11s (credential fix verified via tokenExpiry advance — real driver = inline N+1 sync).

## Wave-2 fixes (07:0x-07:2xZ, all test-first, all delivered)

- W-8 (opus): free-chain-only request shaping — max_tokens (env AETHER_ADMIN_FREE_FALLBACK_MAX_TOKENS, default 2000) + `reasoning:{"enabled":false}` (live probe DISPROVED the brief's `exclude:true` — hides but still generates; only enabled:false works). Latency 60-116s → 1-26s, 6/6 under budget. Ultra-550b outperformed super (10/12 vs 6/12 valid strict JSON) → deploy must reorder prod .env AETHER_ADMIN_FREE_FALLBACK_MODELS ultra-first (AUTHORIZED).
- W-6 (opus): email inbox — TTL-gated sync (AETHER_EMAIL_SYNC_TTL_SECONDS=120, symmetric abs() comparison after discovering hosted-Postgres clock ~3s AHEAD of app clock) + bounded parallel per-thread-client fetch (threading.local, _SerializedCredentials lock, fail-safe sequential). Warm path measured 0.257s (was ~11s); cold ~2.7s modeled. Follow-up filed: sweep codebase for other DB-timestamp-vs-app-clock comparisons.
- W-9 (sonnet): worker logging via arq --custom-log-dict → app/workers/logging_config.py (root INFO, ISO-8601 UTC, arq logger propagate=False). 6 tests + live subprocess format proof.
- W-10 (sonnet): feed.ts honest degraded mapping — completed coverLetter runs with output.coverLetterUnavailable===true → neutral "Unavailable" badge + "cover letter unavailable (generation degraded)" + backend reason; 1,862 historical rows fixed at render. 592/592 vitest.

## INCIDENT: concurrent-orchestrator collision (07:0x-08:1xZ) — RESOLVED

A Hermes kanban board (`aether-job-career-agent`, 3 always-on worker tasks) is ALSO working this repo/backlog. During wave-2 its deploy task: stashed-then-dropped the ENTIRE uncommitted wave-2 tree (destroyed from working tree; survived as dangling stash 4820cc5), re-applied the parked dangerous WIP (agents.py refund-breaking NameError + paywall hole + fabricated tooltip), committed a (harmless, kept) networking ADR e050d8e, REBUILT WEB AFTER RESTARTING SERVICES (07:51 restart / 07:53 build — the §0.3 split-brain class again) — putting the proven-bad tree LIVE with paying users (~25 min; 0 NameError incidents in logs — no error path fired).
RESPONSE: Hermes in-flight work snapshotted (scratchpad hermes-collision/), its 2 remaining repo workers PAUSED via SIGSTOP (reversible; 3rd had exited), tree restored to committed HEAD e050d8e, web rebuilt + all services restarted atomically (verified healthy), wave-2 recovered from the dropped stash (11 files, markers verified; protective branches recovery/wave2-stash-4820cc5 + recovery/w9-commit-8290f8b guard against gc). W-9/W-10 review: merit-PASS (14 pytest + 592 vitest by reviewer; predicate validated against all 2,039 prod AgentRun rows; no gawk double-timestamp — start-worker.sh has no pipe). W-8/W-6 re-review running against recovered diffs.
**OPERATOR DECISION REQUIRED: exactly one orchestrator per repo — pause/retarget the Hermes board, or hand this work off to it. Do NOT resume the paused workers (SIGCONT) into a shared checkout.**

## QA closure #2 (10:15Z): FAIL — reframed critical path → wave-3.5

CLOSED on live evidence: email TTL (cold 5.62s / warm 0.33-0.38s / browser 0.83s), feed honesty (0 false "Drafted"), worker ISO-8601 INFO logging, autopilot runtime bounded (0.12-0.27s ticks), 6-screen regression sweep clean (buildId match), 15-min log window clean.
CORRECTIONS: OpenRouter credits were TOPPED UP (~$26.15 remaining) → 402 scenario gone; free fallback is component-verified (shaped probes: 0 reasoning tokens, valid strict JSON 1.9-3.6s) but UNEXERCISED live — armed for the next 402, not "verified live". Deploy #3 actual time 09:49Z.
NEW CRITICAL FINDINGS:
- **W-11 (CRITICAL, opens when W3-B releases cover_letter_agent.py):** zero real cover letters since 2026-07-26T02:15Z — NOT an LLM outage: healthy 200s, then `FabricationError(claim_flags)` at cover_letter_agent.py:1381 (§9 claim guard) flags JD-domain nouns (marketplace/litigation/JD/trust/safety…) the writer uses aspirationally but the resume doesn't evidence; the claim survives every corrective retry. 07-27: 0/781, 07-28: 0/864, 07-29: 0/209+21 failed. Fix must preserve the honesty bar: distinguish first-person experience claims (resume-evidenced, guarded) from JD-referential motivation language (legitimate), + corrective retry must enumerate flagged tokens. Read [[aether-agent-pipeline-runtime-fixes]] memory notes (PROBABILISTIC guard) before changing.
- **W-12 (HIGH, dispatched):** DB-backed 3-failure suppression has all 6 eligible jobs wedged ~20h with healthy-looking `processed=0` ticks and NO clear mechanism. Fix: auto-clear on success + honest suppressed-until log line + conventional ops clear path.
- **W-13 (MED, dispatched):** inbox payload 723KB/call → limit param (default 50) + list-view snippet truncation, detail path intact.

## Deploy #4 (16:56Z @ 26cea22) + QA #3 (17:26Z): HEADLINE CLOSED, wave-5 opened

W-11 chain 66747b6→1be917e→e05bcbd→26cea22: 4 adversarial rounds, 31/31 attack harnesses, final reviewer PASS. Gates: pytest 1510/1510, vitest 601/601. Deploy: budget 88→150s, restart, un-wedge script cleared 6 jobs.
QA #3 PASS: **first customer-visible letters since 07-26** (2/2 live: Samsara 271 words/35.7s/0 retries + Stripe, all claims resume-traced, honest Drafted badge; pending approvals c14f2338…/c22a0943… for the human gate). Email 96% payload cut + tri-state OK; 15/15 screens 0 console errors; dup-guard WARNING-skip honest (2 violating pairs remain — cleanup pending); 24.5-min logs clean.
QA #3 FAIL residuals → wave-5:
- **W-19 CRIT:** suppression counts only status='failed' but degrades are 'completed' → counter never increments AND floor resets on any completed → autopilot retries same 4 jobs forever (453 letterless runs/24h, real paid calls). Count degraded-completed (coverLetterUnavailable) as failures; floor only on runs WITH cover_letter_id.
- **W-20 HIGH:** "re-enqueued continuation" logged unconditionally; arq dedups same _job_id → silent idle tick (17:08-17:20 observed). Honest enqueue-result logging + working continuation.
- **W-21 MED:** /dashboard/agents shows letterless runs as OK/"92% success" — wire feed.ts honest-badge logic in.
- **W-22 MED:** degraded runs record NULL cost/model/tokens despite real paid calls — record actuals.
- **W-23 MED (W-18 family):** guard re-labelling leak ("audit evidence repository"/"soc 2" 0 resume hits, flagged=[]) — next guard precision round with W-18 anaphora.
- Data cleanup pending: 2 duplicate-application pairs (21 rows) → index activates on cleanup.
Wave-4 (12 agents per AGENTS-IMPLEMENTATION-MATRIX) queued behind wave-5 CRIT/HIGH.

## Wave-5 progress
- W-19/W-20 @ db86cec (pushed): letterless-run predicate (camel+snake, strict jsonb-true) drives all 4 suppression sites + in-memory tick counter + ops script in lockstep; floor only on real cover_letter_id. Live-measured: 3 true-positive jobs now suppressed@3, letter-winning job correctly clear. W-20 RULING: honest-cron UPHELD (suffix-continuation would stack concurrent per-user sweeps — duplicate letters/spend/applications); ≤1 cron period idle accepted; redis-generation-counter variant = follow-up only on live evidence of need. Residual (upstream, → W-23/W-18 round): guard rejections are TRUE positives on those JDs; writer retry convergence for token-mismatched JDs unaddressed.
- W-21/W-22: own evidence green (34/0 backend, 613/613 web incl. honest agents-screen stats); commits pending its final regression sweep.
