# MANUAL-VERIFICATION — Blocked on Human (§9)

The ONLY legitimate non-closures: verifications/decisions requiring operator-held credentials or a business decision the orchestrator must not make unilaterally. Everything testable/fixable without them has been done; each item below is CONDITIONALLY-CLOSED with partial evidence + exact operator instructions.

| # | Item | Why blocked | What the operator must provide/decide | Findings affected |
|---|---|---|---|---|
| H-1 | Operator-admin authenticated screens | `AETHER_ADMIN_EMAIL` + `AETHER_ADMIN_PASSWORD_HASH` absent from prod env; admin/admin123 is a demoted regular user (isAdmin=false). The 6 admin UIs cannot be exercised as a real admin. | Set `AETHER_ADMIN_EMAIL` + bcrypt `AETHER_ADMIN_PASSWORD_HASH` in prod env, restart aether-api, provide the login. (Guard behavior — unauth→401, demoted→403/redirect — IS fully verified.) | admin-* cluster authed views |
| H-2 | Live Stripe payment flow | Checkout degrades honestly (400 "no Stripe price configured" / 429). No live Stripe keys/price IDs/webhook secret in prod → real card→subscription round-trip is untestable and must not be faked. | Provide live Stripe secret key, price IDs, webhook signing secret (or confirm test-mode). Then a real checkout→entitlement→webhook cycle can be verified. | MV-pricing-003, checkout half of pricing |
| H-3 | Legal placeholders — real operator identity | /terms contains live `[Operator ABN]` and `[Business Name]` placeholder brackets (§0.5). The FIX (remove brackets, wire legal links, reconcile AUD/GST) proceeds, but the REAL values must come from the operator. | Provide the operating entity's legal Business Name + Australian Business Number (ABN) + registered address + support contact (email/URL). | MV-terms-002, MV-terms-003, MV-privacy-policy-003 (contact path) |
| H-4 | Free-tier business model (ADR-MV-02 / cluster D) | The product advertises a usable free tier + provisions a free 5-run quota, but the ratified Phase-6 gate blocks all free access. Curing the §0.5 dishonesty is done (marketing honesty + fail-open fix). Whether to instead DELIVER freemium (D1: open dashboard + honor 5 free runs) reverses ratified monetization → owner's call. | Decide: (D2, default, shipped) keep subscription-required + honest "preview" free copy; OR (D1, recommended by evidence) deliver freemium — open read-only dashboard to free users + allow metered runs up to the free quota. Orchestrator recommendation: provisioned quota + loss-leader cost budgeting favor D1. | MV-pricing-001, MV-analytics-001 (business half) |
| H-5 | Anthropic operator token expired | The deployment Anthropic credential (sk-ant-oat01- from Phase-7) returns 401 "OAuth access token has expired" on live verify. Agents still work (route via OpenRouter deepseek/qwen), so no user impact, but the provider card falsely shows "Connected" (MV-agents-004 — the false-green is a code fix; the token refresh is operator-held). | If Anthropic serving is desired, provide a fresh sk-ant-oat01-/sk-ant-api03- token. Otherwise confirm OpenRouter is the intended provider so marketing/model-tier copy can be reconciled (cluster G). | MV-agents-004 (token half), MV-pricing-002 |
| H-6 | Gmail multi-inbox | Gmail IS connected in prod (melbvicduque@gmail.com, 1 account, 64 emails). A SECOND consent would be needed to verify multi-inbox; single-account behavior is verified. | If multi-inbox is in scope, connect a 2nd Gmail via the in-app consent. Otherwise none. | email-center multi-account (not a filed defect) |

All other findings are ADDRESSABLE without operator input and are being remediated in Stage 2. This list is re-verified before exit; nothing here is used to excuse a fixable defect.

## Addendum (2026-07-20, exit phase)

| # | Item | Status change / new info | What the operator should do |
|---|---|---|---|
| H-1..H-6 | (unchanged) | Statuses above re-checked during the exit-phase claim adjudication + re-sample; no operator input arrived during the run, so all six stand as written. | As above. |
| H-7 (FYI, no gate impact) | Owner/demo account + discovery-cron credentials | The 2026-07-18 DB wipe destroyed BOTH seed accounts. During exit remediation the run RESTORED (a) `admin`/admin123 (regular user, isAdmin:false) and (b) `sarkar.vikram@gmail.com` via the app's own demo-seed path — its password is the pre-wipe seed default from the server's `.env` (`LOGIN_PASSWORD`). The discovery cron (aether-discovery.timer) authenticates as that account and is WORKING again (verified live 2026-07-20; sourcing runs hourly). | 1) Sign in as sarkar.vikram@gmail.com with the pre-wipe seed password and CHANGE IT (or delete the account and re-signup — note the cron's `AETHER_CRON_EMAIL`/`AETHER_CRON_PASSWORD` env must then be updated or sourcing dies again, silently no longer: failures now log honestly). 2) Recommended: create a dedicated service account for the cron instead of the owner login. 3) Upload a résumé to that account if fit-scoring in the cron should stop refusing (currently an HONEST 422 refusal each firing). |

## Addendum (2026-07-21, Stripe go-live)

| # | Update |
|---|---|
| H-2 | **RESOLVED — live payments wired.** Owner supplied a LIVE Stripe key (acct_1TvSlM…, AU/AUD, charges+payouts enabled). Set up entirely via the Stripe API: **3 products** (Aether Starter/Pro/Power, each with SaaS tax_code `txcd_10103000`), **6 recurring AUD GST-inclusive prices** (19/179, 39/359, 69/649), a **webhook** → `/api/billing/webhooks/stripe` on the 5 events the handler consumes (signing secret in `.env` as `STRIPE_WEBHOOK_SECRET`), and a **default customer-portal configuration** (update/cancel/invoice-history). Price/product IDs written to BOTH the `Plan` table and `.env` (`STRIPE_PRICE_*`). **App code fix:** `stripe_gateway.create_checkout_session` dropped `payment_method_types` + `automatic_tax` (this account has **Managed Payments** on, which rejects both) — live change in the working tree, **not yet committed to git**. Verified live: `/api/billing/plans` purchasable; `/api/billing/checkout` → real `cs_live_` session (all tiers); `/api/billing/portal` → real `billing.stripe.com` session. **Remaining (Dashboard-only, optional):** (a) upload the crafted gold/black logo under Settings → Branding for exact-match invoices — the API can't set own-account branding, and the account already has generic black/gold set; (b) do one real test purchase to confirm the webhook→entitlement upgrade cycle; (c) confirm GST/Stripe-Tax registration if you're GST-registered. |
| H-3 | **Partially resolved.** From Stripe: `AETHER_OPERATOR_BUSINESS_NAME`="Aether Career Agent" and `AETHER_SUPPORT_EMAIL`="sarkar.vikram@gmail.com" written to `.env`. Legal entity is **Vikram Deshpande** (sole trader). **ABN is still needed** — it is NOT on file in Stripe (`tax_id_provided`=null), so it can't be auto-filled; provide the ABN for the `/terms` legal text (`AETHER_OPERATOR_ABN`). |

## Addendum (2026-07-21, operator-requested account setup)

Two of the items above changed state today at the owner's direction (outside the MANUAL-VERIFICATION run):

| # | Update |
|---|---|
| H-1 | **Partially resolved.** `admin`/`admin123` was promoted directly to a full admin (`User.isAdmin=true`) **and** granted the Pro plan (Subscription `pro`/active + UsageQuota `pro`/100 runs/$15 cap). Verified live: `/auth/me` isAdmin:true, `/api/admin/users` + `/api/admin/health` → 200, entitlement active_paid:true. The **env-based operator-admin mechanism** (`AETHER_ADMIN_EMAIL` + bcrypt `AETHER_ADMIN_PASSWORD_HASH`) is still **not** set — only needed if a separate env-provisioned operator admin is desired on top of the row-level admin. |
| H-7 | **RESOLVED — single consolidated account.** The owner's `sarkar.vikram@gmail.com` was first deleted, then consolidated into the admin account: there is now exactly **one** user — username `admin`, **email `sarkar.vikram@gmail.com`**, `isAdmin=true`, **Pro** plan, password `admin123`. It logs in with EITHER identifier (`admin` or `sarkar.vikram@gmail.com`), both with `admin123` (the login matches `email` exactly OR `lower(username)`). The discovery cron is repointed to it: `AETHER_CRON_EMAIL="sarkar.vikram@gmail.com"` + `AETHER_CRON_PASSWORD="admin123"` in `.env`. Verified live 2026-07-21T01:32Z — firing exits 0 / result:success (login OK → scout 39 jobs → fit-scorer completed). **Operator action:** sign in at the app as `sarkar.vikram@gmail.com` / `admin123` and CHANGE THE PASSWORD (then update `AETHER_CRON_PASSWORD` to match, or point the cron at a dedicated service account). NB: because this email now belongs to the admin account, a separate `/signup` with it is no longer possible — just sign in. |
